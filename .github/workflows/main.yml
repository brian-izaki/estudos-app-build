on:
  workflow_dispatch:

jobs:
  display-variables:
    name: teste
    runs-on: ubuntu-latest
    steps:
    - name: Use variables
      run: |
        echo 'Hi ${{ env.HELLO_WORLD }}'
        echo "TESTE 2 -- ${{ env.TESTE_ENV }}"
